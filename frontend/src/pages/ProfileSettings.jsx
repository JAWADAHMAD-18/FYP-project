import ProfileSettingsSection from "../sections/profileSettings/ProfileSettingsSection.jsx";

/**
 * ProfileSettings page — /profile/settings
 * Renders the ProfileSettingsSection which handles all
 * profile update logic (name, phone, profilePic).
 */
const ProfileSettings = () => {
  return <ProfileSettingsSection />;
};

export default ProfileSettings;
